import type { PlayerType } from "../../Types/playerType";

export interface SelectedPlayersProps {
  player: PlayerType;
}

export default function SelectedPlayers({ player }: SelectedPlayersProps) {
  return <></>;
}
